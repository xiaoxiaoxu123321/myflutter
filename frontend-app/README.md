# Dimensional Frontend

当前 Flutter 前端项目仍在仓库根目录运行。

后续如果需要彻底迁移前端目录，可以把根目录中的 Flutter 文件移动到本目录：

- `lib/`
- `android/`
- `ios/`
- `web/`
- `windows/`
- `macos/`
- `linux/`
- `test/`
- `pubspec.yaml`
- `pubspec.lock`

现在先保留根目录结构，避免影响已经可以安装到手机的 APK 构建流程。
